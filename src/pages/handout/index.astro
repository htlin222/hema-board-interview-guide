---
import { getCollection } from 'astro:content';

const docs = await getCollection('docs');
const topics = docs
	.filter((d) => d.id.startsWith('topics/') && !d.id.endsWith('exam-format-and-mindset'))
	.sort((a, b) => (a.data.sidebar?.order ?? 99) - (b.data.sidebar?.order ?? 99));
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<html lang="zh-Hant">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>一頁速查（可列印）｜血液科口試走向指南</title>
		<link rel="icon" href={`${base}/favicon.svg`} type="image/svg+xml" />
		<style>
			body {
				margin: 0;
				padding: 40px 20px 60px;
				background: #f3f3f3;
				color: #111111;
				font: 15px/1.6 'PingFang TC', 'Heiti TC', system-ui, sans-serif;
			}
			main {
				max-width: 640px;
				margin: 0 auto;
			}
			h1 {
				font-size: 24px;
				margin: 0 0 6px;
			}
			p.lead {
				margin: 0 0 26px;
				color: #555555;
			}
			ul {
				list-style: none;
				margin: 0;
				padding: 0;
				border-top: 1px solid #d4d4d4;
			}
			li a {
				display: flex;
				justify-content: space-between;
				align-items: baseline;
				gap: 12px;
				padding: 11px 4px;
				border-bottom: 1px solid #d4d4d4;
				color: inherit;
				text-decoration: none;
			}
			li a:hover {
				background: #e9e9e9;
			}
			li a span.n {
				color: #666666;
				font-size: 13px;
				white-space: nowrap;
			}
			nav {
				margin-top: 24px;
				font-size: 14px;
			}
			nav a {
				color: #2b2b2b;
			}
		</style>
	</head>
	<body>
		<main>
			<h1>一頁速查（可列印）</h1>
			<p class="lead">
				每章一張橫式 A4：推理架構、每題的破題關鍵句、速記表、容易被電的點。擬答與影片不印——需要細節時回網站看。
				開啟後按「列印 / 存成 PDF」即可。
			</p>
			<ul>
				{
					topics.map((t) => (
						<li>
							<a href={`${base}/handout/${t.id.replace('topics/', '')}/`}>
								<span>{t.data.title}</span>
								<span class="n">列印 →</span>
							</a>
						</li>
					))
				}
			</ul>
			<nav>
				<a href={`${base}/`}>← 回網站</a> ·
				<a href={`${base}/booklet/`}>A4 手冊（全書 21 頁，第一性原理 × MECE × 劑量表）→</a>
			</nav>
		</main>
	</body>
</html>
